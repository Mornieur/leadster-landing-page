import { Meta } from '@storybook/addon-docs';
import { ArgTypes, Story, Canvas } from '@storybook/blocks';
import * as FooterStories from './footer.stories';

# Footer

O `Footer` é um componente utilizado para exibir o rodapé de uma página.

<Canvas of={FooterStories.Primary} sourceState="hidden" />

<Meta title="components/Footer" of={FooterStories} />

O `Footer` é um componente utilizado para exibir o rodapé de uma página. Ele inclui um logotipo e informações da lading page junto com links para redes sociais.

### Exemplo de uso do Footer

```jsx
<Footer />
```
